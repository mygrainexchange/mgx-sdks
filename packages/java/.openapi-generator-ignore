# Files below are hand-written and must survive regeneration.
overlay/**
examples/**
/README.md
/LICENSE
/CHANGELOG.md
# The Java overlay lives in the Maven source tree so the generated pom compiles it.
src/main/java/com/mygrainexchange/mgx/overlay/**
# pom is hand-owned: Central Portal publishing config + io.github groupId + real metadata.
/pom.xml
# No generated test stubs (they don't overwrite, so they go stale and break builds).
src/test/**
