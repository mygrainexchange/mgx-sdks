/* tslint:disable */
/* eslint-disable */
/**
 * MGX Enterprise API
 * Public REST API for grain elevators and enterprise buyers. Browse anonymized inventory, place bids, read your team\'s trades and bids, manage cash bids, subscribe to webhooks, and read market prices. Authenticate with OAuth2 (Login with MGX). Seller identity is never exposed until a trade is paid on both sides.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  OauthRevokePost200Response,
  OauthTokenPost200Response,
  OauthTokenPost400Response,
} from '../models/index';
import {
    OauthRevokePost200ResponseFromJSON,
    OauthRevokePost200ResponseToJSON,
    OauthTokenPost200ResponseFromJSON,
    OauthTokenPost200ResponseToJSON,
    OauthTokenPost400ResponseFromJSON,
    OauthTokenPost400ResponseToJSON,
} from '../models/index';

export interface OauthRevokePostRequest {
    token: string;
    tokenTypeHint?: OauthRevokePostTokenTypeHintEnum;
}

export interface OauthTokenPostRequest {
    grantType: OauthTokenPostGrantTypeEnum;
    clientId: string;
    clientSecret: string;
    refreshToken?: string;
    code?: string;
    redirectUri?: string;
    codeVerifier?: string;
    scope?: string;
}

/**
 * AuthenticationApi - interface
 * 
 * @export
 * @interface AuthenticationApiInterface
 */
export interface AuthenticationApiInterface {
    /**
     * Invalidate a token early — for example on logout or when rotating credentials. Pass `token_type_hint=refresh_token` to also revoke the refresh tokens associated with the access token, preventing further refreshes.
     * @summary Revoke an access or refresh token
     * @param {string} token The token to revoke.
     * @param {string} [tokenTypeHint] Hint for which token type is being revoked.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    oauthRevokePostRaw(requestParameters: OauthRevokePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OauthRevokePost200Response>>;

    /**
     * Invalidate a token early — for example on logout or when rotating credentials. Pass `token_type_hint=refresh_token` to also revoke the refresh tokens associated with the access token, preventing further refreshes.
     * Revoke an access or refresh token
     */
    oauthRevokePost(requestParameters: OauthRevokePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OauthRevokePost200Response>;

    /**
     * Exchange credentials for an access token. Use `grant_type=authorization_code` to complete the Login with MGX flow, `grant_type=client_credentials` for machine-to-machine read-only access, or `grant_type=refresh_token` to obtain a new access token (and a rotated refresh token) before the current one expires. Access tokens live 15 days; refresh tokens live 30 days. This endpoint is unauthenticated — the client credentials are supplied in the body.
     * @summary Issue or refresh an access token
     * @param {string} grantType The OAuth2 grant type.
     * @param {string} clientId 
     * @param {string} clientSecret Required for confidential clients.
     * @param {string} [refreshToken] Required when &#x60;grant_type&#x3D;refresh_token&#x60;. The refresh token returned by a previous token response.
     * @param {string} [code] Required when &#x60;grant_type&#x3D;authorization_code&#x60;. The authorization code returned to your redirect URI.
     * @param {string} [redirectUri] Required when &#x60;grant_type&#x3D;authorization_code&#x60;. Must match the client\\\&#39;s registered redirect URI.
     * @param {string} [codeVerifier] The PKCE code verifier matching the &#x60;code_challenge&#x60; sent to the authorize endpoint.
     * @param {string} [scope] Space-delimited list of scopes. For a refresh, may be a subset of the originally granted scopes.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    oauthTokenPostRaw(requestParameters: OauthTokenPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OauthTokenPost200Response>>;

    /**
     * Exchange credentials for an access token. Use `grant_type=authorization_code` to complete the Login with MGX flow, `grant_type=client_credentials` for machine-to-machine read-only access, or `grant_type=refresh_token` to obtain a new access token (and a rotated refresh token) before the current one expires. Access tokens live 15 days; refresh tokens live 30 days. This endpoint is unauthenticated — the client credentials are supplied in the body.
     * Issue or refresh an access token
     */
    oauthTokenPost(requestParameters: OauthTokenPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OauthTokenPost200Response>;

}

/**
 * 
 */
export class AuthenticationApi extends runtime.BaseAPI implements AuthenticationApiInterface {

    /**
     * Invalidate a token early — for example on logout or when rotating credentials. Pass `token_type_hint=refresh_token` to also revoke the refresh tokens associated with the access token, preventing further refreshes.
     * Revoke an access or refresh token
     */
    async oauthRevokePostRaw(requestParameters: OauthRevokePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OauthRevokePost200Response>> {
        if (requestParameters['token'] == null) {
            throw new runtime.RequiredError(
                'token',
                'Required parameter "token" was null or undefined when calling oauthRevokePost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            headerParameters["Authorization"] = await this.configuration.accessToken("oauth2", ["inventory.read"]);
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            headerParameters["Authorization"] = await this.configuration.accessToken("oauth2", ["inventory.read"]);
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['token'] != null) {
            formParams.append('token', requestParameters['token'] as any);
        }

        if (requestParameters['tokenTypeHint'] != null) {
            formParams.append('token_type_hint', requestParameters['tokenTypeHint'] as any);
        }


        let urlPath = `/oauth/revoke`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OauthRevokePost200ResponseFromJSON(jsonValue));
    }

    /**
     * Invalidate a token early — for example on logout or when rotating credentials. Pass `token_type_hint=refresh_token` to also revoke the refresh tokens associated with the access token, preventing further refreshes.
     * Revoke an access or refresh token
     */
    async oauthRevokePost(requestParameters: OauthRevokePostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OauthRevokePost200Response> {
        const response = await this.oauthRevokePostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Exchange credentials for an access token. Use `grant_type=authorization_code` to complete the Login with MGX flow, `grant_type=client_credentials` for machine-to-machine read-only access, or `grant_type=refresh_token` to obtain a new access token (and a rotated refresh token) before the current one expires. Access tokens live 15 days; refresh tokens live 30 days. This endpoint is unauthenticated — the client credentials are supplied in the body.
     * Issue or refresh an access token
     */
    async oauthTokenPostRaw(requestParameters: OauthTokenPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OauthTokenPost200Response>> {
        if (requestParameters['grantType'] == null) {
            throw new runtime.RequiredError(
                'grantType',
                'Required parameter "grantType" was null or undefined when calling oauthTokenPost().'
            );
        }

        if (requestParameters['clientId'] == null) {
            throw new runtime.RequiredError(
                'clientId',
                'Required parameter "clientId" was null or undefined when calling oauthTokenPost().'
            );
        }

        if (requestParameters['clientSecret'] == null) {
            throw new runtime.RequiredError(
                'clientSecret',
                'Required parameter "clientSecret" was null or undefined when calling oauthTokenPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['grantType'] != null) {
            formParams.append('grant_type', requestParameters['grantType'] as any);
        }

        if (requestParameters['clientId'] != null) {
            formParams.append('client_id', requestParameters['clientId'] as any);
        }

        if (requestParameters['clientSecret'] != null) {
            formParams.append('client_secret', requestParameters['clientSecret'] as any);
        }

        if (requestParameters['refreshToken'] != null) {
            formParams.append('refresh_token', requestParameters['refreshToken'] as any);
        }

        if (requestParameters['code'] != null) {
            formParams.append('code', requestParameters['code'] as any);
        }

        if (requestParameters['redirectUri'] != null) {
            formParams.append('redirect_uri', requestParameters['redirectUri'] as any);
        }

        if (requestParameters['codeVerifier'] != null) {
            formParams.append('code_verifier', requestParameters['codeVerifier'] as any);
        }

        if (requestParameters['scope'] != null) {
            formParams.append('scope', requestParameters['scope'] as any);
        }


        let urlPath = `/oauth/token`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OauthTokenPost200ResponseFromJSON(jsonValue));
    }

    /**
     * Exchange credentials for an access token. Use `grant_type=authorization_code` to complete the Login with MGX flow, `grant_type=client_credentials` for machine-to-machine read-only access, or `grant_type=refresh_token` to obtain a new access token (and a rotated refresh token) before the current one expires. Access tokens live 15 days; refresh tokens live 30 days. This endpoint is unauthenticated — the client credentials are supplied in the body.
     * Issue or refresh an access token
     */
    async oauthTokenPost(requestParameters: OauthTokenPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OauthTokenPost200Response> {
        const response = await this.oauthTokenPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const OauthRevokePostTokenTypeHintEnum = {
    access_token: 'access_token',
    refresh_token: 'refresh_token'
} as const;
export type OauthRevokePostTokenTypeHintEnum = typeof OauthRevokePostTokenTypeHintEnum[keyof typeof OauthRevokePostTokenTypeHintEnum];
/**
 * @export
 */
export const OauthTokenPostGrantTypeEnum = {
    authorization_code: 'authorization_code',
    client_credentials: 'client_credentials',
    refresh_token: 'refresh_token'
} as const;
export type OauthTokenPostGrantTypeEnum = typeof OauthTokenPostGrantTypeEnum[keyof typeof OauthTokenPostGrantTypeEnum];
